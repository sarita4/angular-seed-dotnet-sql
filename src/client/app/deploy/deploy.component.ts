import { Component } from '@angular/core';

/**
 * This class represents the lazy loaded DeployComponent.
 */
@Component({
  moduleId: module.id,
  selector: 'sd-deploy',
  templateUrl: 'deploy.component.html',
  styleUrls: ['deploy.component.css']
})
export class DeployComponent { }
